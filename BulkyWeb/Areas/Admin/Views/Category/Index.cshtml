@model List<Category>


<div class="card bg-dark shadow border-0 mt-4">
    <div class="card-header bg-info ml-0 py-3 rounded-top-2">
        <div class="row">
            <div class="col-12 text-center">
                <h2>List Category</h2>
            </div>
        </div>
    </div>
    <div class="card-body px-4 rounded-bottom-2">
        <div class="container">
            <div class="row pt-4 pb-3">
                <div class="col-12 text-end">
                    <a asp-controller="Category" asp-action="Create" class="btn btn-outline-success">
                        <i class="bi bi-plus-circle"></i> New Category
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-borderless table-striped table-dark">
            <thead>
                <tr>
                    <th>Category Name</th>
                    <th>Display Name</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var category in Model.OrderBy(c => c.DisplayOrder))
                {
                    <tr>
                        <td>@category.Name</td>
                        <td>@category.DisplayOrder</td>
                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Category"
                                   asp-action="Edit"
                                   asp-route-id="@category.Id"
                                   class="btn btn-dark mx-2">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                                <a asp-controller="Category"
                                   asp-action="Delete"
                                   asp-route-id="@category.Id"
                                   class="btn btn-dark mx-2">
                                    <i class="bi bi-trash"></i> Delete
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>
</div>